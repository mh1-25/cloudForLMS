<?xml version="1.0" encoding="UTF-8"?>
<!--
  lmsCommonLib – shared Logback configuration
  ============================================
  • Console : INFO and above, coloured pattern
  • File    : INFO and above, rolling daily, kept 30 days / 500 MB total
  • No DEBUG or TRACE noise from Spring internals
-->
<configuration scan="true" scanPeriod="30 seconds">

    <!-- ── Properties ───────────────────────────────────────────────── -->
    <springProperty scope="context" name="appName"
                    source="spring.application.name"
                    defaultValue="lms-service"/>

    <property name="LOG_DIR"     value="logs"/>
    <property name="LOG_FILE"    value="${LOG_DIR}/${appName}"/>
    <property name="MAX_SIZE"    value="20MB"/>
    <property name="MAX_HISTORY" value="30"/>
    <property name="TOTAL_SIZE"  value="500MB"/>

    <!-- ── Patterns ─────────────────────────────────────────────────── -->
    <!--  Console: coloured, concise  -->
    <property name="CONSOLE_PATTERN"
              value="%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan([${appName}]) %yellow(%logger{36}) - %msg%n"/>

    <!--  File: plain, machine-parseable  -->
    <property name="FILE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [${appName}] [%thread] %logger{40} - %msg%n"/>

    <!-- ── Appenders ─────────────────────────────────────────────────── -->

    <!-- Console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
    </appender>

    <!-- Rolling file – rotates daily, compressed -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- daily + size cap -->
            <fileNamePattern>${LOG_FILE}-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE}</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
    </appender>

    <!-- Separate ERROR-only file for quick triage -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${appName}-errors.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${appName}-errors-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- ── Silence noisy Spring / Hibernate internals ────────────────── -->
    <logger name="org.springframework"                       level="WARN"/>
    <logger name="org.springframework.security"              level="WARN"/>
    <logger name="org.springframework.web"                   level="WARN"/>
    <logger name="org.springframework.cloud"                 level="WARN"/>
    <logger name="org.hibernate"                             level="WARN"/>
    <logger name="org.hibernate.SQL"                         level="INFO"/>   <!-- keep SQL visible at INFO -->
    <logger name="org.hibernate.type.descriptor.sql"         level="WARN"/>
    <logger name="com.netflix"                               level="WARN"/>
    <logger name="feign"                                     level="WARN"/>
    <logger name="com.zaxxer.hikari"                         level="WARN"/>
    <logger name="org.apache.catalina"                       level="WARN"/>
    <logger name="org.apache.tomcat"                         level="WARN"/>
    <logger name="io.netty"                                  level="WARN"/>

    <!-- ── Our application code: INFO ───────────────────────────────── -->
    <logger name="com.lms" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </logger>

    <!-- ── Root (catch-all): WARN ───────────────────────────────────── -->
    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

</configuration>
